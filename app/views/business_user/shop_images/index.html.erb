<div id="page-wrapper">
  <div class="row">
    <div class="col-lg-12">
      <h1 class="page-header">Upload Shops Images</h1>
    </div>
    <!-- /.col-lg-12 -->
  </div>
  <!-- /.row -->
  <div class="row">
    <div class="col-lg-12">
      <div class="panel panel-default">
        <div class="panel-heading">
          <%= link_to "Upload image", new_business_user_shop_image_path(:shop_id => @shop.id), :class => 'btn btn-primary' %>
          <%= link_to "Back", business_user_shops_path, :class => 'btn btn-primary' %>
        </div>
        <!-- /.panel-heading -->
        <div class="panel-body" id="container1">
          
          <div class="row" id="items1">
            <% if !@shop_images.blank?%>
            <% @shop_images.each do |shop_image| %>
            <div class="col-lg-4 col-md-6 col-sm-6 col-xs-12 list">
              <div class="image-wrapper clearfix">
                <%= image_tag shop_image.image_url , :class => "img-responsive" if shop_image.image? %>
                <%= link_to "Delete",
                    business_user_shop_image_path(:id => shop_image.id, :shop_id => @shop.id),
                    :method => :delete,
                    :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                    :class => 'btn btn-mini btn-danger' %>
              </div>
            </div>
            <% end %>
          <% end %>
          </div>
          
          <!-- <div class="dataTable_wrapper">
            <table class="table table-striped table-bordered table-hover dataTables-example" id="dataTables-example">
              <thead>
                <tr>
                  <th>image</th>
                  
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>

                <% if !@shop_images.blank?%>
                <% @shop_images.each do |shop_image| %>
                <tr>
                  <td><%= image_tag(shop_image.image_url) if shop_image.image? %></td>
                  <td>
                    <%= link_to "Delete",
                    business_user_shop_image_path(:id => shop_image.id, :shop_id => @shop.id),
                    :method => :delete,
                    :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                    :class => 'btn btn-mini btn-danger' %>
                  </td>
                </tr>
                <% end %>
                <% end %>
              </tbody>
            </table>
          </div> -->
          <!-- /.table-responsive -->
        </div>
        <!-- /.panel-body -->
      </div>
      <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
  </div>
</div>
<!-- /#page-wrapper -->
